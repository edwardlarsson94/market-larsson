export interface Product {
}

export interface ProductResults {
    id: string,
    title: string,
    image:string,
    name: string,
    description: string,
    price: number,
    availableQuantity:number,
    available: boolean
}
